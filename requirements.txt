# ====================== API / Backend ======================
fastapi==0.110.1
uvicorn==0.29.0
pydantic==2.7.1  # pydantic-core é puxado automaticamente

# ====================== Integração IA ======================
openai==1.30.3
ai21==1.1.1
transformers==4.41.1
accelerate==0.30.1

# ====================== HTTP requests ======================
requests==2.31.0
httpx==0.27.0
aiohttp==3.9.4
python-multipart==0.0.9

# ====================== Texto para fala ======================
gtts==2.5.1

# ====================== Logs ======================
loguru==0.7.0

# ====================== Torch CPU ======================
# Render não tem GPU por padrão, precisa do índice do PyTorch CPU
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.8.0+cpu
